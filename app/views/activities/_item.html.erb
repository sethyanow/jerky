<% if activity.trackable %>
  <img src="<%= activity.owner.image if activity.owner %>" alt="portrait" />
  <h3><%= link_to activity.owner.name, activity.owner if activity.owner %>
  added an item to their cart</h3>
  <%= link_to activity.trackable.flavor.name, activity.trackable %>
<% else %>
removed
<% end %>
